# 2026-04-24 日记

## M-flow Cone Graph 实现（Phase 1 完成）

**核心模块**（新增于 `modules/memory/`）：

| 文件 | 功能 |
|------|------|
| `cone_graph.py` | 四层锥图（Entity/FacetPoint/Facet/Episode）+ SQLite 存储 |
| `coreference.py` | 中英代词消解（摄取时） |
| `granularity_router.py` | 查询粒度路由（Episode/Facet/FacetPoint/Entity） |
| `graph_propagator.py` | BFS 证据链传播 + Path-Cost 评分 |
| `memory_orchestrator.py` | 统一检索编排器（episodic/lexical/unified 模式） |

**Bug 修复**：PRAGMA table_info 返回 `(cid, name, ...)` 误用 `c[0]` → 已修复为 `c[1]`

**数据库**：`memory/cone_graph.db`（SQLite）

---

## 项目学习

### multica-ai/multica
- 自托管 Managed Agents 平台，Go + Next.js 16 + PostgreSQL 17 + pgvector
- 支持 Claude Code / **OpenClaw** / Codex / OpenCode 等多种 Agent CLI
- `make selfhost` Docker 部署，WSL2 必备
- 架构借鉴：Issue/Comment 协作流程 + Skill 沉淀机制

### FlowElement-ai/m_flow
- Bio-inspired Cognitive Memory Engine（Path-Cost 检索）
- 四层锥图：Episode → Facet → FacetPoint → Entity
- 核心洞察：similar ≠ relevant，relevance = 最强证据链
- Benchmark：LoCoMo-10 81.8% / LongMemEval 89%
- 集成方案：`memory/MEMORY-M-FLOW-INTEGRATION.md`

---

## Git 状态
- GitHub 网络持续不稳定
- 本地队列：20013c3 / a4bbb55 / 75e75d0
- 等待网络恢复后 push

---

*Q仔 | 16:55 CST*
