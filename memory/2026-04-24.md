# 2026-04-24 日记

## M-flow Cone Graph 实现（Phase 1 完成）

**核心模块**（新增于 `modules/memory/`）：

| 文件 | 功能 |
|------|------|
| `cone_graph.py` | 四层锥图（Entity/FacetPoint/Facet/Episode）+ SQLite 存储 |
| `coreference.py` | 中英代词消解（摄取时） |
| `granularity_router.py` | 查询粒度路由（Episode/Facet/FacetPoint/Entity） |
| `graph_propagator.py` | BFS 证据链传播 + Path-Cost 评分 |
| `memory_orchestrator.py` | 统一检索编排器（episodic/lexical/unified 模式） |

**Bug 修复**：PRAGMA table_info 返回 `(cid, name, ...)` 误用 `c[0]` → 已修复为 `c[1]`

**数据库**：`memory/cone_graph.db`（SQLite）

---

## 项目学习

### multica-ai/multica
- 自托管 Managed Agents 平台，Go + Next.js 16 + PostgreSQL 17 + pgvector
- 支持 Claude Code / **OpenClaw** / Codex / OpenCode 等多种 Agent CLI
- `make selfhost` Docker 部署，WSL2 必备
- 架构借鉴：Issue/Comment 协作流程 + Skill 沉淀机制

### FlowElement-ai/m_flow
- Bio-inspired Cognitive Memory Engine（Path-Cost 检索）
- 四层锥图：Episode → Facet → FacetPoint → Entity
- 核心洞察：similar ≠ relevant，relevance = 最强证据链
- Benchmark：LoCoMo-10 81.8% / LongMemEval 89%
- 集成方案：`memory/MEMORY-M-FLOW-INTEGRATION.md`

---

## Git 状态
- GitHub 网络持续不稳定
- 本地队列：20013c3 / a4bbb55 / 75e75d0
- 等待网络恢复后 push

---

*Q仔 | 16:55 CST*


---

## M-flow Phase 2 完成：向量嵌入 + 图传播（20:00 CST）

**新增模块**：
- `modules/memory/cone_vector.py`：TF-IDF + FAISS 向量索引（~400行），解决 sklearn 加载时 vocabulary 维度不匹配 bug
- 向量搜索已集成到 `MemoryOrchestrator._find_anchors()`

**验证结果**：
```
Query "deadline communication"
  → Router: facet 层
  → FAISS 向量搜索锚点: fp_5e6bce74085d
  → Graph Propagator: BFS 3 hops
  → Episode reached: ep_a03c0ed4a → score=1.0000
```

---

## M-flow Phase 3 完成：Canvas UI + Skill 封装

**新增文件**：
- `canvas/routing-dashboard.html`：新增 Memory Query 面板（轮询 `memory_query_state.json`）
- `skills/cone-memory/SKILL.md`：Skill 封装文档
- `memory_query_bridge.py`：Python bridge，处理内存查询并写入状态文件

**Canvas UI 功能**：
- 输入查询文本 → 自动轮询结果 → 显示 episode 列表 + 评分 + facet
- 面板：`mem-layer-badge`（粒度层）、`mem-mode-badge`（检索模式）、`mem-count`（结果数）

---

## Git 推送成功

**已推送 commits**（GitHub 网络恢复）：
- `20013c3`：文档更新
- `a4bbb55`：Cone Graph 核心模块
- `75e75d0`：心跳/Heartbeat
- (新) `4 commits`：Canvas UI + cone-memory skill + bridge

**工作树状态**：干净（无未提交变更）

---

## 下一步建议

1. **Invest — FRED API Key**（免费）：`fred.stlouisfed.org/docs/api/api_key.html`，激活真实宏观数据
2. **M-flow Phase 4**：将 `MemoryOrchestrator.query()` 暴露为 Gateway RPC，供 OpenClaw 内部调用
3. **Skill 自动触发**：当用户说 "remember"/"what do you know" 时自动调用 cone-memory

---

*Q仔 | 20:23 CST*
---

## M-flow Phase 4 — Skill + Gateway 集成（进行中，20:55 CST）

**已创建文件**：
- `skills/cone-memory/handler.js`：Skill handler，调用 `memory_query_bridge.py`，支持 episodic/lexical/unified 模式
- `evaluation/locomo_eval.py`：LoCoMo-10 基准评估框架，支持 synthetic sessions 和真实数据集

### Gateway RPC 发现（关键）

Gateway (`openclaw gateway`) 端口 18789，WebSocket 传输，需要 Token 认证（`chen00jie`）。

**重要发现**：`skills/gateway-rpc/SKILL.md` 描述的 `gateway.call tools.catalog` / `methods.map` **在编译后的 JS 中不存在**。Gateway 仅暴露内置方法：
- `health` / `status` / `system-presence`
- `cron.*` 前缀方法
- 无自定义 RPC 方法暴露机制

**正确架构**：Skill 触发走 `SkillDispatcher` → `expert_router.py` → `memory_expert` → skill slug（`cone-memory`）。不是通过 Gateway RPC 实现。

### expert_router.py 更新

已添加 `cone-memory` 到 `memory_expert` 的 skills 列表，并加入触发关键词：
- `/memory`, `search memory`, `query memory`
- `do you remember`, `what do you remember`
- `cone graph`, `m.flow`

### 触发链路

```
User: "/memory deadline communication"
  → SkillDispatcher.match() → /memory trigger
  → expert_router (memory_expert) → cone-memory skill
  → handler.js → memory_query_bridge.py → MemoryOrchestrator.query()
  → Canvas UI / direct output
```

### Skills Health

- avg=90.8（2026-04-24 05:34）
- 无严重问题

---

## 待完成

1. **Skill handler 调试**：确保 `handler.js` 能正确调用 Python bridge 并格式化输出
2. **LoCoMo eval 运行**：`python evaluation/locomo_eval.py`，验证 R@K 指标
3. **invest FRED API Key**：申请免费 key，激活 Fed Funds rate / yield curve / recession probability

---

*Q仔 | 20:55 CST*